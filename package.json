{
  "main": "src/app.js",
  "private": true,
  "scripts": {
    "test": "eslint .",
    "start": "tabris serve -a",
    "gitpod": "tabris serve -a -w --no-intro --port 8080 --external $(gp url 8080):443"
  },
  "dependencies": {
    "jszip": "^3.5.0",
    "tabris": "~3.5.0"
  },
  "devDependencies": {
    "eslint": "^7.4.0"
  }
}
